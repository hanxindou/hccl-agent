# G3-F Narration Script

Canonical semantic source; final language, timing, wording, and voice remain USER_ACTION_REQUIRED.

## NARR-01 / SCENE-01

Collective communication tuning must choose a schedule, explain the decision, and preserve the evidence boundary of every result.

Truth: `RECONSTRUCTED_FROM_FROZEN_EVIDENCE`

## NARR-02 / SCENE-02

The system separates CPU_SIM host execution, deterministic simulation, Agent-assisted decisions, and Direct compile/link-only readiness so that no layer borrows a stronger validation identity; real device execution is not included.

Truth: `CPU_EXECUTED, SIMULATED_ONLY, DIRECT_COMPILE_LINK_ONLY, REAL_DEVICE_NOT_EXECUTED`

## NARR-03 / SCENE-03

A static choice is insufficient across topology and message-scale changes, so the frozen Schedule IR and analytical selector compare explicit schedule candidates under deterministic gates.

Truth: `SIMULATED_ONLY, DETERMINISTIC_EVALUATION`

## NARR-04 / SCENE-04

In the frozen simulated benchmark, the selected schedules achieved 18 wins, 0 ties, and 0 losses. The weighted simulated improvement is 45.59283008 percent raw and 45.59 percent for display; it is not a real Ascend/NPU measurement.

Truth: `SIMULATED_ONLY`

## NARR-05 / SCENE-05

This offline replay shows an Agent-generated proposal, deterministic evaluation, reflection and replanning records, and the final decision. It is replayed from a frozen trace, not presented as original historical autonomous execution, and human governance remains disclosed.

Truth: `AGENT_GENERATED, DETERMINISTIC_EVALUATION, HUMAN_INTERVENTION, OFFLINE_REPLAY, REPLAYED_FROM_FROZEN_TRACE`

## NARR-06 / SCENE-06

The 1024-rank result is a logical model-scale simulation. It is not a measured 1024-card cluster result.

Truth: `SIMULATED_ONLY`

## NARR-07 / SCENE-07

Lossless sparse reconstruction, CRC integrity, and bounded retry are host validated. Sparse wire-byte savings are modeled accounting, while backpressure remains simulated; none of these is NIC or real HCCL hardware validation.

Truth: `LOSSLESS_SPARSE_HOST_EXECUTED, HOST_INTEGRITY_VALIDATED, HOST_RETRY_VALIDATED, SIMULATED_BACKPRESSURE, REAL_DEVICE_NOT_EXECUTED`

## NARR-08 / SCENE-08

The default-off Direct production source contains 17 official ACL/HCCL call expressions and has compile/link-only readiness evidence. No ACL/HCCL runtime, communicator, collective, or real device was executed.

Truth: `DIRECT_COMPILE_LINK_ONLY, DIRECT_READINESS_ONLY, REAL_DEVICE_NOT_EXECUTED`

## NARR-09 / SCENE-09

The live command builds and runs the project-owned CPU_SIM plugin on the host, then replays the Agent trace and verifies registered figures offline. This demonstrates host reproducibility without an API key or NPU and does not rerun the frozen performance benchmark.

Truth: `LIVE_DEMO_CPU_SIM, DEMO_REPLAY, REAL_DEVICE_NOT_EXECUTED`

## NARR-10 / SCENE-10

Real-device acceptance remains hardware blocked. The delivery does not claim real Ascend performance, runtime HCCL execution, measured NIC traffic, or hardware backpressure.

Truth: `REAL_DEVICE_NOT_EXECUTED`

## NARR-11 / SCENE-11

The competition value is an evidence-governed workflow that connects schedule optimization, Agent-assisted decisions, host-validated reliability, simulated performance, and offline reproducibility while keeping every limitation visible.

Truth: `RECONSTRUCTED_FROM_FROZEN_EVIDENCE, REAL_DEVICE_NOT_EXECUTED`
