# G3-B2 ablation report

The initial authoritative attempt produced four wins, one tie, and 13 losses, with a weighted simulated change of -2.02353%. One disclosed evidence-based correction round fixed transport chunk versus Ring shard interpretation, evaluated candidates before selection, and activated an already-defined simulator-only exposed critical path. It did not change the frozen scenario matrix, seed, statistics, correctness thresholds, or model constants.

The final simulator result is 18 wins, zero ties, and zero losses, with a 45.59283008% weighted geometric-mean improvement. Nine priority hierarchical/asymmetric scenarios exceed 10%, no scenario regresses by more than 5%, and correctness, p95, logical-1-GiB, bounded-memory, and no-path gates pass. Ablations retain stages A0 through A7 so the source of modeled improvement remains visible.

This satisfies the internal simulator engineering gate only. Competition performance target achievement remains `PARTIALLY_SATISFIED`; real NPU and real training performance are unmeasured.
